namespace Cleaners.Web.Constants
{
    /// <summary>
    /// App settings constants
    /// </summary>
    public static class AppSettings
    {
        public const string Identity = nameof(Identity);
        public const string AppInfo = nameof(AppInfo);
    }
}