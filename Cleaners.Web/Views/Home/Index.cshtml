@inject IViewLocalizer Localizer
@{
    ViewBag.PageTitle = Localizer[ResourceKeys.Dashboard];
}

<div class="row">
    <div class="col-md-12">
        <div class="callout callout-info">
            <h4>Info!</h4>
            <p>               
                @Localizer["welcome.message", "Feal"]
            </p>
        </div>
    </div>
</div>